//资源使用：

//using stm32F4xx usart2(PA2 and PA3) send and receive analyse
//RS232只是个接口，需要硬件支持，软件写和串口一样，就像USB转TTL ，现在则是TTL转RS232


任务日志：
//mission1: 验证数据准确性  连接上位机  debug看数据 
//测试时间  串口助手定时发送功能  10ms发一次问题不大，所有数据都收到了

//选择需要数据时发出去 ，不用一直接收  后续有精力可考虑验证那种方案好
//波特率115200  接线  黄线 -- 板子的RX   绿线-- 板子的TX

//mission2: 参考代码算出偏航角 ，现在默认为0
//测试航向角对不对  通过pitch roll  
		
		
//接受有问题  只能进一次中断-->需要在回调函数加入		
//	HAL_UART_Receive_IT(&huart2,Rx_Buffer,48);    
//	__HAL_UART_ENABLE_IT( &huart2 , UART_IT_RXNE );  

//mission3 :怎么验证四元数的准确性？

//问题：
//验证数据准确：通过数据手册和数据分析。比如数据手册是-180到180.但没能去验证。